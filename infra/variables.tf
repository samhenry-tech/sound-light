# Input variables for the sound-light infrastructure.
#
# Public shared inputs (project, environment, region, googleClientId, …) are
# read from ../config/shared.json via locals.tf — there are no tfvars required
# for the default single-environment setup. Keep this file for any future
# optional overrides that should stay out of the committed shared JSON.
