# Remote backend is enabled in providers.tf.
#
# State file:
#   s3://projects.samhenry.tech/_terraform/sound-light/terraform.tfstate
#
# That is the shared hosting bucket already used by
# .github/workflows/deploy-frontend.yml — no separate state bucket to create.
# The key stays outside the `sound-light/` SPA prefix so deploy `--delete`
# syncs do not remove it.
