# Remote backend is enabled in providers.tf.
#
# Bucket + lock table bootstrap (also run automatically by CI):
#
#   ./infra/scripts/ensure_remote_state.sh
#
# First-time import of resources created under the old local backend:
#
#   ./infra/scripts/import_existing.sh
