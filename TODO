☒ Set color to the orange theme (removed other themes / the ability to set one)
☒ Remove react query debugging on local
☐ Add a button on local (development) to toggle tablet mode; aside from that don't use the app shell — do it normally with a simple tailwind treatment when tablet mode is on. Put the button in the sidebar
☒ Make it always orange theme, remove the other theme options
☐ Remove sleep timer completely
☒ Make the grid 5 across and always fixed at that
☐ Remove full screen mode/feature
☒ Always use combined labels; remove the ability to change that
☐ Remove the keyboard shortcuts toggle; keyboard shortcuts always on
☐ When adding a Spotify playlist to an in-app playlist, add all of the playlist's tracks to the in-app playlist
☐ Remove the ambient bed feature (we'll change it and move it around later)
☐ Change settings to a cog icon
☐ Add a logout button in the sidebar
☐ Put the tablet mode toggle button in the sidebar
☐ Add a forced popup when not signed into Spotify — force sign-in on arrival at the app
☐ Remove the session title; just have the search box on the left at the top with the chips next to it
